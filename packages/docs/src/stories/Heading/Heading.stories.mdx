import { Meta } from '@storybook/addon-docs'

<Meta title="Typography/Heading" />

# Heading

Por padrão, o Heading se comporta como um `h2`. Porém, podemos alterar livremente o elemento utilizando a propriedade `as`
